import React from 'react';
import ReactDOM from 'react-dom/client';
import BuildFlow from './App.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <BuildFlow />
  </React.StrictMode>
);
